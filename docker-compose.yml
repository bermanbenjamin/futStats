services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: futstats
      POSTGRES_PASSWORD: password
      POSTGRES_DB: futstats
    ports:
      - "5432:5432"
    volumes:
      - futstats_pgdata:/var/lib/postgresql/data

volumes:
  futstats_pgdata:
